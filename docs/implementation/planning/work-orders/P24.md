# P24 — Wire optimization, results and exact comparison transfer

- Issue: [#28](https://github.com/MaximilianMauroner/dps.lol/issues/28)
- Issue body revision: `2026-09-18T20:10:40Z`
- Owner lane: UI integration worker
- Prerequisites: P16, P19, P22, P23
- Owned paths: `src/app/page.tsx` (exact transfer to P29 requires sender-side
  `handoffTo: ["P29"]`), `src/components/lab/optimizer.tsx`,
  `src/components/lab/results/**`, `src/components/lab/compare-adapter/**`, `tests/ui/flows/**`

## Deliverables

Replace hardcoded champion/patch state with generic controllers and real defaults/cohorts. Connect
worker evaluation, next-purchase/full-build flows and honest ranked alternatives: objective, cost,
coverage, TTK/censoring, uncertainty, scope and search status. Transfer exact selected build/state,
resolved scenario, policy, cohort weights and random configuration into comparison atomically.

## Acceptance

Selected result reproduces its score and hash in comparison. New edits cannot replace old result
state. The complete user flow works with real adapters and explicitly labeled fixture fallback,
warnings and trace entry points.
