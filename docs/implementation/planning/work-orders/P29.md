# P29 — Integrate, migrate and validate the whole product

- Issue: [#33](https://github.com/MaximilianMauroner/dps.lol/issues/33)
- Issue body revision: `2026-09-18T20:10:46Z`
- Owner lane: Final integrator + independent QA
- Prerequisites: P19, P24, P25, P26, P27, P28
- Owned paths: current public facades and UI integration listed in issue #33, with the exact
  `src/app/page.tsx` handoff from P24. P29 declares `handoffFrom: ["P24"]`, and this transfer is
  valid only when P24's matching `src/app/page.tsx` claim declares `handoffTo: ["P29"]`; no
  broader UI ownership is transferred. Also owns `tests/e2e/**`,
  `docs/implementation/final-validation.md`, and product docs.

## Deliverables

Complete the public-facade migration, wire explanations/replay, remove the independent Yunara
execution loop and retire obsolete coverage wording/duplicate implementations. Run end-to-end and
all unit/property/interaction/observation/worker/replay/benchmark checks. Test additive migrations,
isolated archive rebuild/restore, authentication, validation, unsafe-script boundaries, credential
exposure and result-state consistency. Update docs to actual capabilities.

## Acceptance

One engine powers every calculation surface, the complete journeys and storage recovery are real,
and documentation distinguishes implementation, empirical evidence, data readiness and deployment
authorization. P29 owns `page.tsx` only after P24 handoff.
