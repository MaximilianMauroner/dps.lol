# P00 — Pin and characterize the existing application

- Issue: [#4](https://github.com/MaximilianMauroner/dps.lol/issues/4)
- Issue body revision: `2026-09-18T20:10:12Z`
- Owner lane: Advisor + baseline worker
- Prerequisites: none
- Owned paths: `docs/implementation/baseline.md`, `tests/regressions/legacy-characterization/**`,
  `benchmarks/baseline/**`, `docs/implementation/planning/**` except the future
  `docs/implementation/planning/CONTENT_TASKS.json` and
  `docs/implementation/planning/CONTENT_MANIFEST.json` handoff paths, `scripts/check-plan.ts`.
  The planning envelope records `handoffTo: ["P02"]` for those two carve-outs.

## Deliverables

1. Pin the exact commit/tree and compare the isolated checkout to the audited main baseline.
2. Run the existing check and optimizer benchmark; record commands, exit statuses, runtimes,
   fixture/real-cohort provenance, hardware/runtime, counts and unchanged failures.
3. Add a credential-free regression for future W-tick chronology and characterize cooldown waits,
   expected crit state, result transfer and current inventory validation without blessing the W bug.
4. Record product requirements, archive/storage safety, request/storage ceilings, proposed targets
   and the agreed benchmark matrix without ingesting or mutating production data.
5. Materialize the reviewed roadmap, coordination rules, all parent work orders, content template
   and machine-readable graph. Validate IDs, mappings, prerequisites, cycles and ownership.

## Acceptance and disposition

The W chronology assertion is intentionally red at the audited baseline and is isolated/documented
as a P00 regression. P04/P08/P16 own its eventual repair through the shared engine. Any pre-existing
environment failure receives a named disposition in `baseline.md`; later check reports retain it.
P00 does not modify source runtime code, dependencies, lockfiles, production data or deployment.

## Handoff

The next integration action is acceptance of P01's single-owner contracts and migration boundary.
