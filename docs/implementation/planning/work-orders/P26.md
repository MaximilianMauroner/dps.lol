# P26 — Add portable replay and bounded large-search execution

- Issue: [#30](https://github.com/MaximilianMauroner/dps.lol/issues/30)
- Issue body revision: `2026-09-18T20:10:42Z`
- Owner lane: Replay/operations worker
- Prerequisites: P02, P18, P22
- Owned paths: `src/domain/reproducibility/**`, `src/jobs/optimization/**`, `scripts/optimize.ts`,
  `scripts/replay-simulation.ts`, `src/db/jobs/**`, `migrations/<reserved-jobs>_*.sql`,
  `tests/replay/**`, `tests/jobs/**`

## Deliverables

Create canonical hashed replay bundles containing ruleset bytes, scenario, candidate, cohorts/
weights, policy, random configuration, engine build and search certificate. Provide a bounded Bun
CLI and optional authenticated server adapter using the same engine. Add job ownership, quotas,
leases, heartbeat, retries, checkpoints, cancellation, idempotency and expiry without secrets or
raw participant IDs. Retain executable artifacts, not only hashes.

## Acceptance

Sanitized downloads replay without fetching current static/cohort data. Browser, worker and CLI
agree for deterministic runs and seed sets. Remote job machinery remains opt-in and bounded.
