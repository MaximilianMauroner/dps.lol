# P02 — Pin source artifacts and enumerate complete ruleset coverage

- Issue: [#6](https://github.com/MaximilianMauroner/dps.lol/issues/6)
- Issue body revision: `2026-09-18T20:10:14Z`
- Owner lane: Ruleset/data-source worker
- Prerequisites: P01
- Owned paths: `docs/implementation/planning/CONTENT_TASKS.json`,
  `docs/implementation/planning/CONTENT_MANIFEST.json` (future paths handed off from P00),
  `src/domain/rulesets/**`, `scripts/sync-static.ts`, `scripts/rulesets/**`, `tests/rulesets/**`.
  P00 retains the rest of `docs/implementation/planning/**` and excludes the two exact content
  artifacts from its broad planning claim. P00 records `handoffTo: ["P02"]`; each exact content
  path records `handoffFrom: ["P00"]`.

## Deliverables

Pin PC patch/hotfix, Data Dragon, CommunityDragon, mode and regional mappings while retaining
26.18/16.18.1. Archive source bytes and hashes, compile typed catalogs and a mechanic evidence
ledger, and enumerate every required champion/form/ability/item/rune/summoner/environment/mode
variant. Generate stable child IDs, `docs/implementation/planning/CONTENT_TASKS.json`,
`docs/implementation/planning/CONTENT_MANIFEST.json` and the mechanic dependency graph only after
artifact enumeration and review. These two exact planning paths are P02's materialization
boundary; P00's baseline/planning documents remain unchanged by this later generation.

## Acceptance

Identical artifacts produce identical manifests; changed bytes or mappings change the hash. Missing
sources, conflicts and untracked mechanic IDs block publication. A named catalog is not marked
combat-complete merely because it has names, costs and stats. P02 owns the content fan-out.
